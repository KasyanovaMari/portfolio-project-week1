import styled from "styled-components";
import {theme} from "../styles/Theme.ts";

export const SectionTitle = styled.h2`
    font-weight: 600;
    font-size: 24px;
    letter-spacing: 0.04em;
    color: ${theme.colors.headlineFont};
    margin-bottom: 20px;
`;
